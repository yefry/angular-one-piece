export class DemonFruit {
    name: string;
    description: string;
    model?: string;
    urlImage?: string;
    color?: string;
    relatedFruit?: string;
    power?: string; // it can be level too or puntuaction
    history?: string;
}
